{% extends 'base.html.twig' %}

{% block body %}
    <h3>Time to learn!</h3>

    {% for composer in composers %}
        <h4><a href="{{ path('learn_composer', { 'slug': composer.slug }) }}">{{ composer.name }}</a></h4>
    {% endfor %}
{% endblock %}

{% block title %}
    Quizellaneous - Learn
{% endblock %}
